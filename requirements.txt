# RAG Demo 依赖包
# 安装命令: pip install -r requirements.txt

# 句子转换库，用于将文本转为向量
sentence-transformers>=2.2.0

# 向量数据库1: ChromaDB (最简单易用)
chromadb>=0.4.0

# 向量数据库2: FAISS (Meta开源，高性能)
faiss-cpu>=1.7.4

# 向量数据库3: Milvus Lite (轻量版，无需服务器)
# 注意: pymilvus>=3.0 后需安装 milvus_lite 扩展:
#   pip install "pymilvus[milvus_lite]"
pymilvus>=2.3.0

# 科学计算库，用于自己实现向量数据库
numpy>=1.24.0
